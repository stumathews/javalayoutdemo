package javaguishowcase;

import java.awt.Container;

/**
 *
 * @author Stuart
 */
public class JavaLayoutShowcase {

       public static void main(String[] args) {
        // Lets go ahead and construct a MainForm object. Its a subclass of JFrame
        // and thus should show up when you make it visible, which we will do.
        
        MainForm mainForm = new MainForm();
        mainForm.setVisible( true ); // This shows the form to the user
        
        // If you want to interact with the visible portion of the form from outside,
        // you need to obtain the form's content pane, which when manipulated 
        // changes the form in the way you want
        
        Container contentPane = mainForm.getContentPane();
        
    }
}
